<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External storage root -->
    <external-path name="external_files" path="." />
    <!-- App files directory -->
    <files-path name="app_files" path="." />
    <!-- App cache directory -->
    <cache-path name="app_cache" path="." />
    <!-- Shared storage -->
    <external-files-path name="shared_storage" path="." />
</paths>
